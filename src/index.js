import React from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import ReactAppWrapper from './ReactAppWrapper.jsx';

createRoot(document.getElementById('root')).render(<ReactAppWrapper />);
